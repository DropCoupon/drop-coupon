spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dropcoupon
    username: dropcoupon
    password: dropcoupon
    driver-class-name: org.postgresql.Driver
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092
